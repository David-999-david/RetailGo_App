import 'package:flutter/material.dart';

class LocationPlaces extends StatelessWidget{
  const LocationPlaces({super.key});

  @override
  Widget build(BuildContext context) {
  return SizedBox();    
  }
}